# Token Identity Verification - API Environment Variables

# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tokenverify?schema=public"

# CORS (comma-separated origins)
CORS_ORIGIN=http://localhost:3000

# API URL (for documentation)
API_URL=http://localhost:3001

# Logging
LOG_LEVEL=debug

# Development Mode (enables /v1/dev/seed endpoint for demos)
# Set to 'true' for local development, 'false' for production
DEV_MODE=true

# =============================================================================
# AUTHENTICATION (Milestone 2)
# =============================================================================

# Admin Token (required for creating projects and API keys)
# Generate a secure random token: openssl rand -hex 32
# If not set, admin endpoints return 404
ADMIN_TOKEN=

# =============================================================================
# BUILD INFORMATION
# =============================================================================

# Git commit hash (for /v1/status endpoint)
# Usually set by CI/CD pipeline
GIT_COMMIT=

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================

# GitHub API Token (optional, but recommended for higher rate limits)
# Create a token at https://github.com/settings/tokens
# No special permissions needed for reading public repos
GITHUB_TOKEN=

# DNS Servers (optional, comma-separated)
# Defaults to system DNS if not set
# DNS_SERVERS=8.8.8.8,1.1.1.1

# RPC URLs (optional, overrides default public endpoints)
# Format: RPC_URL_{CHAIN_ID}
# RPC_URL_1=https://mainnet.infura.io/v3/YOUR_KEY
# RPC_URL_137=https://polygon-mainnet.infura.io/v3/YOUR_KEY
